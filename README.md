# elliptic-curves
Elliptic Curves

## TODO

- Provide different implementations of calculating multiplicative inverses.
Currently only Euclidean distance algorithm is used.
- Fix `bit(i)` security vulnerability in _double-and-add_ algorithm in EC. 
Montgomery ladders or always-add logarithm should be used.
- Provide different implementations of EC (Jakobine, etc.) not just `secp256k1`.
